/*import {FIND_TITLE} from '../constants/action-types'

const serachreducer = ( state = 'w', action ) => {
  if (action.type === 'WALID' ) {
     console.log('hhhhhhhhhhhhhhhhhh')
      return  { ...state , aa : 'hh'}
  }
  else {
    return state
  }
}

export default   serachreducer*/